What is Git?

    -> Git is a distributed version control system. It let's you track changes made to a file
       and allows us to revert back to any particular change that you wish to. 

    -> It is a distributed architecture, that provides many advantages over other version control
       systems like the SVN.

    -> One of the major advantage is that it does not rely on a central server to store all the versions
       of project files. Instead every developer clones a copy of the repository






What is a distributed version control system?

    -> These are the systems that don't rely on a central server to store a project file and all it's versions.

    -> In such a VCS, every programmer can get a local copy of the repo using cloning, using pull and push operations
       the local repository and the server repositories can be kept in sync.







What are the advantages of VCS?

    -> Provides flexibility. Multiple programmers can work on the same thing without affecting each other's work.

    -> All the versions are easily available to all programmers with access to the repository.

    -> Changes can be tracked easily

    -> Provides backup







what is the difference between Git and SVN?

    -> Git is a decentralized version control tool, SVN is a centralized version control tool

    -> SVN does not allow a local cloning facility.

    -> In git commits are possible even if we are offline, but in SVN only online commit is possible.
       So push and pull operations are quicker in Git








What is the difference between Git and GitHub?

    -> Git is a version control system. 

    -> GitHub is an american company that provides hosting for software development version control using Git. It offers all 
       of the distributed version control and source code management (SCM) functionalities of Git as well as some extra features.









What language was used in creating Git?

   -> Git uses 'C' language. Git is fast, and 'C' language makes this possible by reducing the overhead of run times associated 
      with high-level languages









Mention the various Git repository hosting functions

   -> GitHub
   -> GitLab
   -> Bitbucket
   -> SourceForge
   -> GitEnterprise








What is a repository in Git?

   -> Repository is where git stores all the files, be it local or remote repository.












What is a bare repository in Git

   -> Contains info about the version control and it does not contain the special .git subdirectory.
      It contains all it's contains directly in the main director











What is commit message?














How do you fix a broken commit?

   -> git commit amend












How do you resolve a conflict in Git

   -> 












How do you revert a commit that has already been pushed and made public?

   -> There can be two approaches to tackle this problem.

   -> Remove the mistakes and commit push it again

   -> Using git revert name_of_commit








What is SubGit?

   -> SubGit is a tool for SVN to Git migration. It can create a writable git mirror of a local or remote subversion
      repository and use both subversion and Git as long as you like.











What is the difference between git pull and fetch?

   -> 












What is a staging area or index?

   -> Before completing the commits, any change canbe formatted and reviewed in an intermediate area known as the staging area.












What is the use of git instaweb?

   -> 'git instaweb' is used to automatically direct a web browser and run a webserver with an interface into your local repo.







-----------------------------------------Finished beginner level questions-----------------------------------------------------------




What is git is-tree?

   -> Represents a tree object including the mode and the name of each item and the SHA-1 value of the tree.












What is git stash?

   -> Stashing takes your working directory that is, your modified tracked files and staged changes and saves 
      it on a unfinished changes that you can reapply at any time.











What is the function of git stash apply?

   -> If you want to continue working where you left your work then 'git stash apply' command is used to bring back
      the saved changes onto your current working directory.











What work is restored when the deleted branch is recovered?

   -> The files which were stashed and saved in the stash index list will be recovered back. Any untracked files will be lost.












What is the difference between git status and git diff?

   -> 'git diff' depicts the changes between commits, commit and working tree etc. whereas 'git status' shows you the difference
      between the working directory and the index, it is helpful in understanding git more comprehensively.











Difference between git clone and git remote?

   -> git remote add, creates an entry in your git config that specifies a name for a particular URL

   -> git clone, creates a new repository by copying an existing one located at the URL.










